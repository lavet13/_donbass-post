Where Ivan is with bash (as of this session)
Comfortable and tested: strict mode (set -euo pipefail), variables + quoting, all expansion forms (:- :+ :? +x), if testing exit codes, [ ] vs [[ ]], test conditions (string/file/number), &&/||, arithmetic $(( )), redirection (> >> 2> &> 2>&1 | <<<), here-docs/here-strings, arrays + [@]/[*] (understands the quoting distinction), for/while/case, functions with local, special vars, glob vs regex matching, command builtin, ./source, argument-parsing template, curl | bash -s -- delivery.
Added to notes but only lightly exercised: tee, process substitution <(cmd), trap + $LINENO/$FUNCNAME (parked as "error-trap only — low priority").
Built two real scripts: setup-debian.sh (Debian provisioning — Docker install, UFW, deploy+splinter users via a create_user_with_ssh function, opt-in swap and --harden-ssh) and health-check.sh (Approach A: runs locally, SSHes via an ssh_exec helper, loops containers, tracks failures with a counter, exits 1). Pending polish: move config to a sourced health-check.env (+ committed .example).
Learning style: wants every line/flag/symbol explained, fires many granular follow-ups, keeps this bash-knowledge.md. Prefers reproducing behavior manually over trusting it.
