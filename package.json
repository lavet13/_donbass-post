{
  "name": "donbass-post-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "turbo run preview",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types"
  },
  "packageManager": "yarn@1.22.22",
  "devDependencies": {
    "turbo": "^2.7.2"
  }
}
